// import {query, URL} from '../utils/dataRequest' export par default

export default function Client({name, email, phone}) {
    // query(URL)
  return (
    <li>{name} | {email} | {phone}</li>
  )
}

